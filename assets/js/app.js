$(document).ready( {
});

/*cuando se aprete el boton glyphicon-menu-right o se aprete la tecla ENTER, solo si es que hay texto en el input.
se envie el texto ingresado en un div vacio que estara en la seccion CONVERSATIONS*/
var newDiv = $('.newDiv')

$('.container-fluid').append(newDiv) 
    ;